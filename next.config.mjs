/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/global-edtech-platform',
  assetPrefix: '/global-edtech-platform/',
  images: { unoptimized: true }
};

export default nextConfig;
